"""Skills shipped with ${dist_name} — the `bos.skills` entry-point target.

Each skill is a subdirectory containing a SKILL.md file. BOS discovers them
in this project and in any project that installs this package.
"""
