"""${tool_name} — project-local tool, auto-discovered via [platform.extensions]."""

from bos.core import ep_tool


@ep_tool(
    name="${tool_name}",
    description="TODO: one line telling the agent what ${tool_name} does and when to use it.",
    parameters={
        "type": "object",
        "properties": {
            "text": {"type": "string", "description": "Input for the tool."},
        },
        "required": ["text"],
    },
)
async def ${func_name}(text: str) -> str:
    # TODO: implement. The returned string is what the agent sees.
    return f"${tool_name} received: {text}"
